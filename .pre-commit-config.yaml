repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.0 
    hooks:
      - id: gitleaks
        name: Run Gitleaks pre-commit secret scan
        entry: gitleaks detect --source . --no-git --verbose --redact
        language: golang
        pass_filenames: false
